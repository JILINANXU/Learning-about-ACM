#include <bits/stdc++.h>
using namespace std;
#define For(i,a,b) for(int i=(a);i<=(int)b;i++)
#define ll long long
inline void solve(){
	int n;
	int ans = 0;
	scanf("%d",&n);
	For(i,1,n){
		int a;
		scanf("%d",&a);
		ans ^= a;
	}
	if(!ans) printf("No\n");
	else printf("Yes\n");
	return;
}
int main(){
	int T;
	scanf("%d",&T);
	while(T--) solve();
	return 0;
}
